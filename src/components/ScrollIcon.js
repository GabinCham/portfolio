import React from 'react';
import '../styles/ScrollIcon.css'; // Assurez-vous d'avoir le bon chemin vers votre fichier CSS

const ScrollIcon = () => {
  return (
<div className="arrow"></div>
  );
};

export default ScrollIcon;
